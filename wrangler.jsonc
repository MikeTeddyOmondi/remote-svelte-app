/**
 * For more details on how to configure Wrangler, refer to:
 * https://developers.cloudflare.com/workers/wrangler/configuration/
 */
{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "remote-svelte-app",
  "main": ".svelte-kit/cloudflare/_worker.js",
  "compatibility_date": "2026-07-30",
  "compatibility_flags": ["nodejs_compat", "nodejs_als"],
  "assets": {
    "binding": "ASSETS",
    "directory": ".svelte-kit/cloudflare"
  },
  "observability": {
    "enabled": true
  },
  /**
   * Attaches the custom domain on deploy. Requires `locci.cloud` to be a zone
   * on this same Cloudflare account.
   *
   * The host must sit under `locci.cloud` for Locci sign-in to work: the
   * OpenAuth issuer at auth.locci.cloud uses the default `allow`, which permits
   * a redirect_uri only when its hostname matches the issuer's last two labels
   * (or is localhost). `remote-svelte-app.mt0.dev` was rejected with
   * `unauthorized_client` for exactly this reason.
   */
  "routes": [
    {
      "pattern": "remote-svelte-app.locci.cloud",
      "custom_domain": true
    }
  ],
  "upload_source_maps": true,
  /**
   * Smart Placement
   * https://developers.cloudflare.com/workers/configuration/smart-placement/#smart-placement
   */
  // "placement": {  "mode": "smart" }
  /**
   * Bindings
   * Bindings allow your Worker to interact with resources on the Cloudflare Developer Platform, including
   * databases, object storage, AI inference, real-time communication and more.
   * https://developers.cloudflare.com/workers/runtime-apis/bindings/
   */
  "d1_databases": [
    {
      "binding": "DB",
      "database_name": "remote-svelte-app",
      "database_id": "521f8cda-b887-479f-8007-8bf9001aca6c",
      // Drizzle writes migrations here; `wrangler d1 migrations apply` is the
      // only path that reaches the LOCAL miniflare database.
      "migrations_dir": "drizzle"
    }
  ],
  "vars": {
    // Production origin; overridden by .dev.vars locally. Better Auth derives
    // its cookies, CSRF checks, and the OAuth redirect_uri from this, so it has
    // to move in lockstep with `routes` above:
    // https://remote-svelte-app.locci.cloud/api/auth/oauth2/callback/locci-auth
    "ORIGIN": "https://remote-svelte-app.locci.cloud",
    // Public OAuth identifier, not a secret — it travels in the authorize URL.
    // Also what `isLocciEnabled` keys off to render the sign-in button.
    // The client secret stays unset: Locci is a public PKCE client.
    "LOCCI_CLIENT_ID": "remote-svelte-app"
  }
  /**
   * Environment Variables
   * https://developers.cloudflare.com/workers/wrangler/configuration/#environment-variables
   * Note: Use secrets to store sensitive data.
   * https://developers.cloudflare.com/workers/configuration/secrets/
   */
  // "vars": {  "MY_VARIABLE": "production_value" }
  /**
   * Service Bindings (communicate between multiple Workers)
   * https://developers.cloudflare.com/workers/wrangler/configuration/#service-bindings
   */
  // "services": [  {   "binding": "MY_SERVICE",   "service": "my-service"  } ]
}
